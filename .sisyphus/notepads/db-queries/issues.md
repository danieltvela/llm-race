# issues — db-queries test scaffolding

## Unresolved
- No pyproject.toml or setup.py — project has no standard Python packaging. Consider adding one for `uv pip install -e .` to work.
- LSP (basedpyright) not installed — diagnostics unavailable for Python files.